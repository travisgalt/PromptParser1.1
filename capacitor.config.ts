import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.promptparser11',
  appName: 'PromptParser1.1',
  webDir: 'dist'
};

export default config;
